class Solution:
    def differenceOfSums(self, n: int, m: int) -> int:
        dif = 0
        for i in range(1, n+1):
            if i%m == 0:
                dif -= i
            else:
                dif += i
        return int(dif)
        
