class Solution:
    def toLowerCase(self, s: str) -> str:
        lower = ""
        for i in range(len(s)):
            if 65 <= ord(s[i]) <= 90:
                lower += chr(ord(s[i])+32)
            else:
                lower += s[i]
        return lower
            
        
