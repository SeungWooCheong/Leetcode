class Solution:
    def prefixCount(self, words: List[str], pref: str) -> int:
        count = 0
        for w in words:
            if len(w) < len(pref):
                pass
            elif w[0:len(pref)] == pref:
                count += 1
            else:
                pass
        return count
