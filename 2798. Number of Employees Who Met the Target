class Solution:
    def numberOfEmployeesWhoMetTarget(self, hours: List[int], target: int) -> int:
        meet = 0
        for i in range(len(hours)):
            if hours[i] >= target:
                meet += 1
            else:
                pass
        return meet 
