class Solution:
    def countDigits(self, num: int) -> int:
        snum = str(num)
        cd = 0
        for i in range(len(snum)):
            if num%int(snum[i]) == 0:
                cd += 1
            else:
                pass
        return cd
                
                
