class Solution:
    def accountBalanceAfterPurchase(self, purchaseAmount: int) -> int:
        if purchaseAmount%10 <= 4:
            return 100 - 10*(purchaseAmount//10)
        else:
            return 90 - 10*(purchaseAmount//10)
