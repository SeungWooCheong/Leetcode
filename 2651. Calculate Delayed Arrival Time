class Solution:
    def findDelayedArrivalTime(self, arrivalTime: int, delayedTime: int) -> int:
        da = arrivalTime + delayedTime
        while True:
            if da >= 24:
                da -= 24
            else:
                return da
            
        
