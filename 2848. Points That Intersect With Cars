class Solution:
    def numberOfPoints(self, nums: List[List[int]]) -> int:
        dic = {}
        for n in nums:
            for i in range(n[0], n[1]+1):
                if i not in dic:
                    dic[i] = 1
                else:
                    pass
        return len(dic)
